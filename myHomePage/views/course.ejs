<% include ./partials/header.ejs %>
<div class="container">
        <div class="card text-white bg-secondary mb-3">
                <div class="card-header"><%= studies.name %><span class="small"> <%= studies.points %></span></div>
                <div class="card-body">
                  <p class="card-text"><%= studies.description %></p>
                  <hr class="my-4">
                  <p class="small">linneaus university <%= studies.endDate %></p>
                </div>
            </div>
</div>
<% include ./partials/footer.ejs %>