<% include ./partials/header.ejs %>
<div class="container">
        <% if (user != null) { %>
    <a class="btn btn-primary" href="/studies/add" role="button">Add</a>
        <% } %>    
    <h4>Linneaus University</h4>
    <ul class="list-group">
    <% for(let i=0; i<studies.length; i++) { %>
            <% if (studies[i].institute === "Linneaus University") { %>
                <a href="/studies/<%= studies[i].id%>" class="list-group-item list-group-item-action"><%= studies[i].name %></a>
            <% } %>
    <% } %>
    </ul>
    <h4>Nackademin</h4>
    <ul class="list-group">
    <% for(let i=0; i<studies.length; i++) { %>
            <% if (studies[i].institute === "Nackademin") { %>
                <a href="/studies/<%= studies[i].id%>" class="list-group-item list-group-item-action"><%= studies[i].name %></a>
            <% } %>
    <% } %>
    </ul>
    <h4>Luleå Technical University</h4>
    <ul class="list-group">
    <% for(let i=0; i<studies.length; i++) { %>
            <% if (studies[i].institute === "Luleå Technical University") { %>
                <a href="/studies/<%= studies[i].id%>" class="list-group-item list-group-item-action"><%= studies[i].name %></a>
            <% } %>
    <% } %>
    </ul>
    <h4>Jensen Vocational College</h4>
    <ul class="list-group">
    <% for(let i=0; i<studies.length; i++) { %>
            <% if (studies[i].institute === "Jensen Vocational College") { %>
                <a href="/studies/<%= studies[i].id%>" class="list-group-item list-group-item-action"><%= studies[i].name %></a>
            <% } %>
    <% } %>
    </ul>
</div>
<% include ./partials/footer.ejs %>
