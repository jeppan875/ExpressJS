<% include ./partials/header.ejs %>
    <div class="container">
                <h2><%= works.company %><h2>
                <div>
                  <p class="small"><%= works.startDate %> - <%= works.endDate %></p>
                  <p><%= works.description %></p>
                  <hr class="my-4">
                </div>
                <% if (user == null) { %>
                <a class="btn btn-primary" href="/works/edit/<%= works.id%>" role="button">Edit</a>
                <a class="btn btn-danger" href="/works/delete/<%= works.id%>" role="button">Delete</a>
                <% } %>
    </div>
<% include ./partials/footer.ejs %>
